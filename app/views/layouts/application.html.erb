<!DOCTYPE html>
<html>
  <head>
    <title>Depodoc</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= tag :meta, name: :psj, action: action_name, controller: controller_name %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <!-- %= favicon_link_tag 'favicon.ico' %-->
   
  </head>

  <body>

    <nav class="navbar navbar-expand-md navbar-light bg-light">
    
        <!-- http://v4-alpha.getbootstrap.com/components/navbar/#how-it-works  -->
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <%= fa_link_to('-file-invoice-dollar', sessions_path, class: 'navbar-brand') {'Depodoc - Депозитарні документи'} %>

      <div class="collapse navbar-collapse" id="navbarSupportedContent" >
        <ul class="navbar-nav mr-auto">


          <%= mainmenu_item( {
              current: request.path, 
              menuitem: root_url, 
              active: true
          }) {'Документи'} %>

          <%= mainmenu_item( {
              current: request.path, 
              menuitem: root_url, 
              active: true
          }) {'Операції'} %>


          <%= mainmenu_dropdown( {
            current: request.path, 
            menuitems: {
                          'Користувачі' => [root_url,true],
                          'Ролі' => [root_url,true],
                        }
          }) {"Налаштування"}  %>

        </ul>

        
        
      <div id="user-zone">
        <!-- %= render 'users/user_zone' %-- >  
      </div>
        
      </div><!-- /.navbar-collapse -->

      

      
    </nav>


    
    <% flash.each do |name, msg| %>
      <%= content_tag :pre, msg, id: "flash#{name}", class: "notice" %>
    <% end %>  

    
    <%= yield %>


  </body>
</html>
