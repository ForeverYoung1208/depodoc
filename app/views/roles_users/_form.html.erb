<%= form_with(model: roles_user, local: true) do |form| %>
  <% if roles_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(roles_user.errors.count, "error") %> prohibited this roles_user from being saved:</h2>

      <ul>
        <% roles_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
