<p id="notice"><%= notice %></p>

<h1>Faces</h1>

<table>
  <thead>
    <tr>
      <th>Code</th>
      <th>Name</th>
      <th>Note</th>
      <th>Is fiz</th>
      <th>Is resident</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @faces.each do |face| %>
      <tr>
        <td><%= face.code %></td>
        <td><%= face.name %></td>
        <td><%= face.note %></td>
        <td><%= face.is_fiz %></td>
        <td><%= face.is_resident %></td>
        <td><%= link_to 'Show', face %></td>
        <td><%= link_to 'Edit', edit_face_path(face) %></td>
        <td><%= link_to 'Destroy', face, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Face', new_face_path %>
