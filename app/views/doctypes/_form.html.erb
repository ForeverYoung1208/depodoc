<%= form_with(model: doctype, local: true) do |form| %>
  <% if doctype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doctype.errors.count, "error") %> prohibited this doctype from being saved:</h2>

      <ul>
        <% doctype.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

    <div class="field">
    <%= form.label :isComplex %>
    <%= form.check_box :isComplex %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
