<%= form_with(model: opstate, local: true) do |form| %>
  <% if opstate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(opstate.errors.count, "error") %> prohibited this opstate from being saved:</h2>

      <ul>
        <% opstate.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :possible_changes %>
    <%= form.text_area :possible_changes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
