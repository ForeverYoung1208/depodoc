<%= form_with(model: log, local: true) do |form| %>
  <% if log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(log.errors.count, "error") %> prohibited this log from being saved:</h2>

      <ul>
        <% log.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :changed_model %>
    <%= form.text_field :changed_model %>
  </div>

  <div class="field">
    <%= form.label :changed_id %>
    <%= form.text_field :changed_id %>
  </div>

  <div class="field">
    <%= form.label :from_value %>
    <%= form.text_field :from_value %>
  </div>

  <div class="field">
    <%= form.label :to_value %>
    <%= form.text_field :to_value %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
